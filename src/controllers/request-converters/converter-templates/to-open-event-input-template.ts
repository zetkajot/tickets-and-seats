import { ConverterFactorySettings } from '../make-input-converter';

const toOpenEventInputTemplate: ConverterFactorySettings = [
  {
    argumentName: 'id',
    desiredName: 'eventId',
  },
];

export default toOpenEventInputTemplate;
