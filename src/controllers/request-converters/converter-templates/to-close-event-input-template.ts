import { ConverterFactorySettings } from '../make-input-converter';

const toCloseEventInputTemplate: ConverterFactorySettings = [
  {
    argumentName: 'id',
    desiredName: 'eventId',
  },
];

export default toCloseEventInputTemplate;
